<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Tile="clr-namespace:MyCathedra.Controls.Tile;assembly=MyCathedra.Controls"
    x:Class="MyCathedra.MainWindow"
    mc:Ignorable="d"
    Title="{Binding Content.Title, ElementName=MainFrame, StringFormat=Дипломная работа: \{0\}}" Height="700"
    Width="1024" WindowStartupLocation="CenterScreen" Loaded="Window_Loaded" Background="#FFEDEEF0">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="275" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" Background="#FFE3E4E6"
                      Grid.Column="0">
            <StackPanel Name="BasePanal" />
        </ScrollViewer>
        <DockPanel x:Name="NavigationPanel" Grid.Column="1">
            <Frame x:Name="MainFrame" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                   Style="{DynamicResource {x:Type Frame}}" />
        </DockPanel>
    </Grid>

</Window>